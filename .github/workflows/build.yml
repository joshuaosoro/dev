name: Node Continous Integration
on: push
jobs: 
    build-job:
        name: Build Node Project
        runs-on: ubuntu-latest
        steps:            
            - run: node --version
            - run: npm --version
            - run: pwd